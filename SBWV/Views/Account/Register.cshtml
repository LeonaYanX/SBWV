@using Microsoft.AspNetCore.Mvc.TagHelpers;

@model SBWV.Models.Register

<link rel="stylesheet" href="~/css/Register.css" />

<div style="text-align:center">@TempData["Message"]</div>

<div id="catalog" class="register">
    
        @using (@Html.BeginForm("Register", "Account", FormMethod.Post))
        {
            @Html.EditorForModel()
            <br />
            <span>&nbsp;</span>
            <span>&nbsp;</span>


            <div class="item" style="text-align:center">
                <div class="file-input-container">
                    <label class="file-input-label" for="register">Зарегистрироваться</label>
                    <input id="register" class="file-input" type="submit" />
                </div>

            </div>

        }
    
   
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

